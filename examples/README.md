# Examples

Example projects will be added later.

Future examples may include:

- a vibe coding setup project (shared memory across Claude Code and Codex sessions)
- a memory update example (how `remember`, `reflect`, and `reflect-apply` flow into durable memory)
- a single-card dispatch example (a `ready` board card run through `dispatch`, then reviewed)

Examples should avoid private project or user data unless it is explicitly cleared for public use.
