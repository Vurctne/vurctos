# Profile: claude (Orchestrator)

- tier: orchestrator
- channel: native
- responsibilities: run the canonical loop, decompose the request into cards, assign profiles, coordinate handoff, review results, update memory and skills
- reviewed by: the user
- handoff: not applicable; the Orchestrator writes and reads the board

## Rules

- Prefer to delegate rather than do all the work directly.
- Never review your own execution output. Assign code review to codex.
- Keep the board and handoffs current so the project is understandable from files alone.
