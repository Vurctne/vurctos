# Profile (moved)

Durable profile memory now lives in `USER.md`.

This file is kept only as a pointer for older references. Put who the user is and how they like to work, project facts, conventions, constraints, and decision patterns in `USER.md`. See `docs/memory-system.md` for the three-layer memory model.
